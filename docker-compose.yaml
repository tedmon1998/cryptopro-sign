version: '3.7'

services:
  container_name: cryptopro
  cryptopro:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        CERTIFICATE_PIN: 'testcer'
        LICENSE: ''
    ports:
      - "3037:3037"
    restart: unless-stopped
